require("./react_ujs.js");

(function (w) {

  w.React     = require("react");
  w.ReactDOM  = require("react-dom");

  // We don't need to require *every* React component in this file, just the
  // ones that are required at the 'top level' by react_ujs
  w.CardAccountApplyOrDecline = require("./CardAccountApplyOrDecline");
  w.CardApplicationSurvey     = require("./CardApplicationSurvey");
}(window));
