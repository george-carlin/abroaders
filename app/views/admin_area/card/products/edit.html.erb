<% provide :title, "Edit Card ##{@product.id}" %>

<div class="hpanel">
  <div class="panel-heading hbuilt">
    <h1>Edit Card</h1>
  </div>
  <div class="panel-body">
    <%= render 'form', card: @product %>
  </div>
</div>
