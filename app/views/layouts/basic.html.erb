<% layout = cell(Abroaders::Cell::Layout, nil, flash: flash, title: title(yield(:title))) %>
<!DOCTYPE html>
<html>
  <%= layout.head %>
  <body class="blank">
    <%= layout.navbar %>
    <%= layout.noscript %>
    <%= layout.flash_alerts %>

    <%= yield %>

    <%= cell Abroaders::Cell::Footer %>
    <%= cell(Abroaders::Cell::ThirdPartyScripts) %>
  </body>
</html>
