<!DOCTYPE html>
<html>
  <%= cell(Abroaders::Cell::Layout::Head, nil, title: title(yield(:title))) %>
  <body class="blank">
    <%= cell Abroaders::Cell::Navbar, current_user %>
    <%= cell(Abroaders::Cell::Noscript) %>

    <%= flash_alerts %>
    <%= yield %>

    <%= cell Abroaders::Cell::Footer %>
    <%= render "shared/third_party_scripts" %>
  </body>
</html>
