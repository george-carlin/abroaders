var id      = <%= @balance.id %>;
var value   = "<%= @balance.value %>";
var isValid = <%= @valid %>;

if (isValid) {
  window.BalancesIndex.saveNewBalance(id, value);
} else {
  window.BalancesIndex.showErrorMessage(id);
}
