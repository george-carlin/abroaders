<% provide :title, @user.name %>

<div class="row">
  <div class="col-xs-12">
    <h1><%= @user.name %></h1>

    <p>Signed up on <%= @user.created_at.strftime("%D") %></p>

    <% if @user.survey.nil? %>
      <%= user_email_with_icon(@user) %>

      <%= t("admin.users.show.no_survey") %>
    <% end %>

  </div>
</div>

<% if @user.survey.present? %>
  <hr style="margin-bottom:0;">

  <%= render "surveys/survey", user: @user %>

  <hr>

  <p>
    Cards -
    <%= link_to "Recommend", new_admin_user_card_recommendation_path(@user) %>
    |
    <%= link_to "Assign", new_admin_user_card_path(@user) %>
  </p>

  <p class="help-block">
    <i>
      If you 'recommend' a card to a user, they will receive a notification and
      the card will appear under 'recommended cards', where they will be able
      to apply for the card, decline the recommendation, etc. If you 'assign' a
      card then you will be able to choose the exact status (accepted,
      declined, denied) etc of the account, and the card will appear in the
      user's list of cards without them receiving a notification.
    </i>
  </p>
<% end %>

<%= link_to 'Back to all users', admin_users_path %>
