<div class="hpanel">
  <div class="panel-heading">
    <h1>Manage Admins</h1>
  </div>
  <div class="panel-body">
    <table class="table table-striped">
      <thead>
        <tr>
          <th>ID</th>
          <th>Email</th>
        </tr>
      </thead>
      <tbody>
        <% model.each do |admin| %>
          <tr>
            <td><%= admin.id %></td>
            <td><%= escape!(admin.email) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
