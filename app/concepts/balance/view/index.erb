<div class='hpanel'>
  <div class="panel-heading hbuilt">
    <h1>
      Balances
      <%= link_to_add_new_balance unless account.couples? %>
    </h1>
  </div>

  <% people_with_balances.each do |person, balances| %>
    <%= balances_for_person(person, balances) %>
  <% end # people.each %>
</div>
